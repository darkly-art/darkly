name: Effects
description: Brushes that move and reshape the pixels already on the canvas.
icon: mdi:blur
palette:
  chroma: "#8b5cf6"
  refraction: "#e05ccb"
  surface: "#1b1922"
  ink: "#d8cbee"
members: [blur, liquify, smudge]
