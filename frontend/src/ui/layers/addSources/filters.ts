import { effectLayerSource } from './effectLayer';

/** The Filters half of the effect catalog — per-texel colour adjustments. */
export const source = effectLayerSource('newFilterLayer', 'Filters');
